---
{"publish":true,"created":"2025-11-24T19:41:06.000+03:00","modified":"2025-12-04T00:03:50.000+03:00","cssclasses":""}
---


|                        |                                                                                                           |
| ---------------------- | --------------------------------------------------------------------------------------------------------- |
| ![[Источники/Портреты/Мужской.jpg|Мужской.jpg]] | **Пантелеев Стефан Алексеевич / 1772-\- / [[Места/Холмогоры/Сухарево\|Сухарево]]** |

**Отец**: Пантелев Алексей Киприанов сын (1733)
**Мать**: Пантелева Акилина (1740)
**Супруг**: `=this.Супруг`
**Дети**:  Петр,Михаил,Григорий,Афанасий,Ксения
**Комментарий**: `=this.Комментарий`

# Источники
```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => p.file.inlinks.includes(dv.current().file.link))
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `${page.file.name} (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```

```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => {
    const персоны = p["Персоны"];
    if (!персоны) return false;
    return Array.isArray(персоны) 
      ? персоны.some(link => link.path === dv.current().file.path)
      : персоны.path === dv.current().file.path;
  })
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `[[${page.file.name}]] (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```
